# 隔离 Codex 构建平面与运行平面

格致允许项目构建与日常语义执行使用同一 ChatGPT 账户和 Codex 模型，但二者必须属于不同执行平面。构建平面在源码工作区内按开发授权运行；运行平面只使用项目锁定的 CLI，在单次临时工作区中以临时会话、忽略用户配置与规则、最小只读权限、关闭非必要工具以及严格输出 Schema 运行，只能读取显式输入包，不能读取或修改源码仓库。两者只共享登录身份和账户额度；退出登录、限流或服务不可用只会把运行时语义阶段标记为阻塞，不得污染构建会话、修改正式资产或触发模型回退。
