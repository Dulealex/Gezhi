# 将格致建模为可扩展的多上下文产品

格致是唯一的产品、仓库和 CLI 身份；Literature 与 Knowledge 是首批领域上下文，PaperBot 与 KnowledgeBot 只作为旧系统迁移对照，不进入新代码的产品命名。后续 Bot 以拥有独立语言和状态的同级上下文加入，并通过显式合同协作，而不是复制基础设施或直接写入其他上下文；扩展位置由稳定边界预留，不通过提前创建空 Bot 实现。这个选择降低重复配置与编排成本，同时要求共享产品发布时持续维护上下文隔离。
