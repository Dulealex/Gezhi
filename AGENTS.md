## Agent skills

### Issue tracker

Issues、PRDs 和实施 tickets 存放在 `Dulealex/Gezhi` 的 GitHub Issues，并通过 `gh` CLI 管理。See `docs/agents/issue-tracker.md`.

### Triage labels

使用 MATT 默认五角色标签。See `docs/agents/triage-labels.md`.

### Domain docs

采用由根目录 `CONTEXT-MAP.md` 导航的多上下文结构。See `docs/agents/domain.md`.
